---

import {db, eq, Artista, Usuario, Session, Normal, Audios, Album_Audio, Album, Usuario_MeGusta_Audio} from 'astro:db'

const userId = "xzm8bp1y7"
await db.delete(Session).where(eq(Session.userId, userId));
await db.delete(Normal).where(eq(Normal.idUsuario, userId));
await db.delete(Artista).where(eq(Artista.idUsuario, userId));
await db.delete(Usuario).where(eq(Usuario.id, userId));

// const artistaId = "rhrpqcl5e"
// await db.delete(Artista).where(eq(Artista.idArtista, artistaId));

// const idAudio = "cnyip2e8c"
// await db.delete(Album_Audio).where(eq(Album_Audio.idAudio, idAudio));
// await db.delete(Audios).where(eq(Audios.idAudio, idAudio));

// const idAlbum = "0iyik5col"
// await db.delete(Album).where(eq(Album.idAlbum, idAlbum));

// const idEscucha = "3mpgb8drm"
// await db.delete(Usuario_Escucha_Audio).where(eq(Usuario_Escucha_Audio.idEscucha, idEscucha));

// const idMegusta = "iu4fam1wp"
// await db.delete(Usuario_MeGusta_Audio).where(eq(Usuario_MeGusta_Audio.idMegusta, idMegusta));

---

